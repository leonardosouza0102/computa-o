#include<stdio.h>
int main()
{
    int hora, min, seg;

    printf("Digite hora, minutoe segundo:");
    scanf("%d %d %d", &hora, &min, &seg);
    if(hora>1 && min>15);
    printf("quantidade de segundos: %f\n", (hora*3600.0)+(min*60.0+seg);
           (
               else
               )
               printf("quantidade de minutos: %f\n",(hora*60.0)+min+(seg\60.0));


               return 0;
    }
